# n8n Dockerfile for Railway deployment
FROM n8nio/n8n:latest

# n8n runs on port 5678 by default
EXPOSE 5678

# Railway will automatically set PORT env var, but n8n uses N8N_PORT
# The default port 5678 works fine with Railway's automatic port mapping

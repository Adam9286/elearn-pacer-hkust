# n8n Environment Variables for Railway
# Copy these to Railway dashboard → Your Service → Variables tab

# n8n Configuration
N8N_HOST=n8n.learningpacer.org
N8N_PORT=5678
N8N_PROTOCOL=https
N8N_CORS_ORIGIN=*

# PostgreSQL Database Connection
# Railway provides these as template variables: {{PostgreSQL.HOSTNAME}}, etc.
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST={{PostgreSQL.HOSTNAME}}
DB_POSTGRESDB_PORT={{PostgreSQL.PORT}}
DB_POSTGRESDB_DATABASE={{PostgreSQL.DATABASE}}
DB_POSTGRESDB_USER={{PostgreSQL.USER}}
DB_POSTGRESDB_PASSWORD={{PostgreSQL.PASSWORD}}

# Optional: Encryption key for n8n credentials (generate a random string)
# N8N_ENCRYPTION_KEY=your-random-32-character-string-here
